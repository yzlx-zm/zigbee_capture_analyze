# S4 — 报文页稳定化 (审查+修复+复验闭环)

**What to build:** 打包前稳定化 (使用流程序): 报文页 code-review + bug 排查, P0/P1 清零 + 用户抽验; 稳定化完成后处理遗留优化项。

**Blocked by:** None | **Status:** 进行中

**Assignee:** Claude (2026-08-26) — 认领

**Type:** task | **稳定化系列** (总纲 S0)

## 需求基线 (审查的"需求轴"对照)

- U16 时间线看包体验优化 (Resolution): 7 项落地 + 用户驱动附加 (页面改名报文 / 全量化 / MAC Ack / 自动加载 / DataReq 命名 / 🔒 仅 NWK 安全)
- U5 时间线优化 (Resolution): 类型下拉动态化 / 事件标记 / 详情修复 (nwk undefined / 联动时间全零) / 跳转节点过滤

## 素材集

- 验证可用-记录/1-标准入网抓包-2.cubx — 基础导入
- 中继入网抓包(1).cubx — 含密文/Leave/路径/上行反转 (U16 实测素材)
- 2-群控压测问题包.cubx — 多设备/APS Ack 密集

## 审查方式

1. /code-review skill (规范轴 + 需求轴) — frontend/js/timeline.js + backend/api/files.py
   (/api/packets 列表 /packets/{id} 详情 + summary/layer/路径/事务链段) + backend/aps_pairing.py
   (事务链构建) + cubx_reader/tshark 的 MAC Ack 解析段
2. CDP 实测: 报文页加载/过滤/未解密开关/摘要列/路径列/APS Ctr/层级着色/详情面板/字段点选/
   事务链跳转/分页 → 边界: 空数据/未解密素材/大包翻页/未导入状态
3. 与导入页联动: 导入后自动进报文页 (自动加载全量)

## 历史遗留对照 (逐条验证)

- U16 遗留 1: 上行路径 = 下行反转 (对称假设, RR 证据不足) — 验证是否如实标注
- U16 遗留 2: pcap 路径 MAC Ack 解析未实测 — 有 pcap 素材则实测
- U16: 事务链跨页高亮 (不在当前页时跳转翻页后高亮) — 验证
- U5: 详情 TypeError (nwk undefined) 修复端到端 — 待含 MAC 帧素材
- 通用: test_p1_contract check 3/4/5 基线失败 (非本次引入)

## 验收标准

- P0 (崩溃/白屏/功能错) = 0; P1 (功能缺陷) = 0; P2 体验问题记录汇总
- CDP 截图证据 + 用户抽验: 报文页完整流程确认

## 产出

- Resolution: 问题清单 (分级) + 修复记录 (提交号) + 复验证据 + 用户抽验结果
- 后端共享文件 (files.py) 改动记录: 改了什么函数/端点 (防并行冲突)

## Resolution (2026-08-26)

**结论**: 审查 (静态+CDP) → 修复 → 复验闭环完成。**P0 = 0, P1 = 0 (2 项全修), P2 修 1 项**。提交 e127191。

### 问题清单 (分级)

**P1 (功能缺陷, 2 项 — 全部修复)**:
| # | 问题 | 实锤 | 修复 |
|---|------|------|------|
| 1 | **事务链构建 O(n²)**: 每 ZCL 命令帧遍历全部 packets — 群控包 10.8 万帧点详情卡死数分钟 (旧版 8435 帧对比跑 5 分钟+ 未完成) | 代码级 + 对比实测 | 索引化: 响应候选按 (src,dst,tsn) 分组, 查询 O(1); 语义逐位一致 (e127191) |
| 2 | **cubx 详情 Security 层整层缺失**: fallback 构造把安全头放层树顶层, timeline 只查 nwk 内 → 解密 key/帧计数/MIC 不显示 | CDP 实测 (中继包详情只有 MAC/NWK/APS/ZCL) | 顶层+nwk 双查 (e127191) |

**P2 (体验, 1 项 — 修复)**:
| # | 问题 | 修复 |
|---|------|------|
| 1 | 详情标题帧号与表格"帧号"列不一致 (列表索引 vs 抓包原始帧号) | 标题同时显示抓包帧号 (e127191) |

**P2 记录不修/待素材**:
- pcap 路径 MAC Ack: 419 帧识别 + fcf/seq 提取实测通过; **pending=1 位素材无此场景** (44KB pcap 无间接传输) — 待素材, 如实标注
- 事务链跨页高亮: 跳转定位实测正常 (tlJumpFrame 翻页/清过滤兜底)
- 大包性能: 178 万帧自动加载全量 (前端每次请求全量扫描) — P3 范畴, 已知

### 复验证据

- CDP s4_confirm: 10/14 (4 ❌ = Security 缺失 P1 实锤 + 脚本插值 bug + 分页/隐藏现象, 后续单独实测澄清)
- CDP s4_verify: 7/8 — Security 层 ✓ / 详情帧号 ✓ / 事务链显示+跳转定位 ✓ / 层级着色 ✓ / 未解密开关 ✓; 唯一 ❌ (路径列) 为脚本时序, 单独实测 '0xED6F→0x5694→0x0000' 渲染正常
- **性能实证**: 群控包 108324 帧详情 (事务构建) 0.2s (修复前 5 分钟+ 未完成); 中继包 8435 帧 0.1s
- **功能实证**: 事务链帧 id=722 (1 响应 + ack=726); 路径列 1168 帧 (API 全量扫描); pcap MAC Ack 419/870 帧
- **回归**: zcl_fcf 12/12 / p1 15/15 / parser_verify 12/12 (aps_pairing 改动无副作用)
- 脚本: `.scratch/verification/s4-timeline/` (s4_confirm / s4_verify)

### 用户抽验

- ⏳ **待用户**: 报文页完整流程 (自动加载 → 过滤 → 未解密开关 → 点帧详情含 Security 层 → 事务链跳转 → 路径列 → 分页) + 群控包点详情不卡

### 用户逐层核对补充修复 (2026-08-26, 提交 cf562ba)

用户在抽验中给出 Ubiqua 逐层结构 (Data Request FCF 0x8863 + Ack 0x0002), 逐层核对发现并修复 **2 个 P1**:

| # | 问题 | 实锤 | 修复 |
|---|------|------|------|
| 1 | **FCF 截断**: cubx 路径只保存 frame_type (低 3 位), 详情 FCF 显示 0x0003 而非真实 0x8863 (security/ack-req/pan-comp/寻址模式高位全丢, 双路径不一致) | 逐层对照: poll 帧 0x0003 vs Ubiqua 0x8863 | cubx_reader 保存完整 2 字节 mac_fcf + fallback 显示完整值; 验证 "MAC Cmd [0x8863]" ✓ |
| 2 | **ZCL 命令误标**: GP 帧 (0x0021/EP242, cluster-specific) cmd 0x02 被前端全局表兜底误标 "Write Attributes" — 用户以为该帧是写入属性 | 官方依据 (Silicon Labs SDK command-id.h, GP spec gp-1.0a-09-5499-26): 0x02 = **GP Proxy Commissioning Mode** | zcl_defs 补 GP 命令表 (7 命令, 注明 SDK 方向标注矛盾) + 前端 _tlZclCmd 加 **frame_type 守卫** (cluster-specific/厂商不猜, 显示原始 ID) + detail 透出 zcl_frame_type + 详情标题精确类型 (ZCL 命令名优先); 验证 "ZCL GP Proxy Commissioning Mode" ✓ |

**核对结论**: poll/Ack 帧其余字段 (Seq/PAN/地址/CMD ID 4=DataReq / Ack FCF 0x0002/seq 沿用) 与 Ubiqua 结构逐项一致 ✓; 回归 3 套全过; 版本号 v20260826d/v20260827f。

### poll 帧误导修复补充 (2026-08-26, 提交 5f95de1)

用户抽验反馈 2 项, 已修:
1. **P1: poll/Beacon/Ack 纯 MAC 明文帧详情误显 "APS/ZCL 🔒 Encrypted"** — decrypted=False 是"无安全层"而非"加密未解" (U16 全量化后列表 🔒 已修, 详情漏同步); detail 透出 nwk_security, Encrypted 提示加 NWK 安全守卫
2. **poll 帧路径列 "—" 无信息量** → 标注 MAC 层地址 ("0x737D→0x0000", DataReq tooltip "MAC 层轮询: 设备→父节点")
验证: CDP s4_poll_verify 4/4 (路径列 0x737D→0x0000 / 详情仅 MAC 层无 Encrypted) + 回归 3 套; 版本号 v20260826e/v20260827g

### 后端共享文件 (files.py) 改动记录

- 本次**未改 files.py** (改动在 aps_pairing.py 事务链构建 — files.py 的 /packets/{id} 详情端点消费方, 缓存接口不变)
- 连带: timeline.js (Security 层/帧号) / aps_pairing.py (索引化)
- **cf562ba**: cubx_reader.py (mac_fcf) / files.py (_detail_dict zcl_frame_type + fallback FCF) / zcl_defs.py (GP 命令表) / timeline.js (frame_type 守卫 + 标题类型)

### 遗留优化项 (用户提示"还有一部分优化项", 稳定化后处理)

- U16 遗留 1: 上行路径 = 下行反转 (对称假设, RR 证据不足) — 需 RR 素材或用户确认
- pcap MAC Ack pending=1 待素材
- 178 万帧自动加载性能 (P3)
- 其余待用户列出具体优化清单

### 用户需求追加 (2026-08-27)

1. **整行层级着色** (提交 ff43311): 对齐确认 3 点 (①整行文字着色无背景 ②选中行优先级更高 ③未解密按层级着色) — CSS .tl-row-* 整行 td 文字色 + 摘要去淡背景 + .hl td 深色优先; CDP 验证: ZCL 行 7 列同色 rgb(22,101,52) / 摘要背景 transparent / 选中行 rgb(31,41,55)
2. **清除数据联动** (提交 904a435): 导入页清除数据 → 报文页 PAN/节点/时间/类型过滤 + 拓扑联动状态全部重置; CDP 验证 6/7 (无数据空态不自动加载 = 正确行为)
