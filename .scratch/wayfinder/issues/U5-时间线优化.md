# U5-时间线优化 — UI 工程

**Assignee:** claude-session 2026-08-05

**What to build:** 时间线页 (585 行) 优化: 筛选交互/事件标记 (Leave/Rejoin/NetworkStatus)/历史跳转 bug 修复。

**Blocked by:** None

**Status:** ready-for-agent

**Type:** task | **HITL/AFK**

**UI 工程背景**: 见 docs/ui_overview.md (UI 工程 wayfinder 拆分地图)

## Resolution (2026-08-05)

**范围** (用户确认): 三方向全做 + 用户补充 2 个 bug (详情加载 TypeError / 联动时间参数全零)。事件标记的协议依据来自芯科文档 MCP (Leave 命令标志位) — MCP 工具查询中途故障 (后续查询 No such tool available, 已记录), Network Status 状态码沿用工程内已验证表 (L3-5 ticket 07 实证)。

**产出** (timeline.js + files.py + app.css):
1. **详情加载崩溃修复** — `Cannot read properties of undefined (reading 'zbee_nwk.fcf_tree')` 根因: tlShowDetail 中 `for(var ck in nwk)` 在 `if(layers.zbee_nwk)` 块外, 非 NWK 帧 (Beacon/ACK/MAC Cmd) 无 zbee_nwk 层 → TypeError → 整帧详情加载失败。修复: nwk 判空包裹 (timeline.js:377-383)
2. **联动时间参数全零修复** — 根因: S.topoT0/T1 契约不统一 (timeline 存 "HH:MM:SS" 字符串 / topo 存数字时间戳), `new Date("10:00:00"*1000)`=NaN → 下拉 NaN/全零; 旧坏值 "NaN" 不被区间比较捕获。修复: 新增 `tlToTs()` 兼容双格式 (延迟到 import/status 回调, tlCaptureStart 就绪后转换) + `isNaN(savedSec0)` 兜底 + 拓扑跳转清空时间窗口时重置为抓包全范围
3. **跳转 bug 修复 (Bug A)** — 拓扑点击节点跳时间线后节点框为空 (S.topoAddr 未同步 tlNode)。修复: init 同步 `S.topoAddr→S.tlNode`
4. **事件标记** — Leave (rejoin 区分 ⛔离网/🔄重入网) / Rejoin Request+Response (🔄) / Network Status (⚠️状态) 行内徽章, 设计系统 token 配色 (.badge-ev/.badge-leave/.badge-rej/.badge-nstat); 后端 GET /packets 输出加 nwk_cmd_id/nwk_leave_rejoin/nwk_leave_request/nwk_leave_children (双路径解析器已输出, 无解析改动)
5. **筛选交互** — 类型下拉硬编码 13 类型 → /api/packets/types 全量统计 (新端点, 带计数); 页面加载即填充 (不依赖点查看), search() 内兜底

**验证** (playwright Edge headless + curl, 素材 leave_question_packet.pcap 986 包):
- ✅ 类型下拉 20 项 (19 类型+全部) 带计数
- ✅ 徽章 DOM 断言: badge-leave:⛔离网 (Leave rejoin=0, 与 L1-4 台账一致) + badge-nstat:⚠️状态
- ✅ Leave 帧 #160 / Network Status 帧 #53 详情面板正常加载
- ✅ 跳转: 拓扑点节点 → #tl, tl-node=0x0000 自动同步 (修复前为空), 时间下拉=抓包全范围 (10:19:29~10:21:13, 非全零), 自动搜索生效 (共 1213 包 节点=0x0000)
- ⚠️ **详情崩溃端到端未触发**: 现有素材均无缺 NWK 层的帧型 (Beacon/ACK 查询 0 条), 该修复为代码级验证 (root cause 明确 + node --check) — 待含 MAC 帧素材确认
- 验证脚本: .scratch/verification/u5-timeline/verify_u5.js + verify_u5_jump.js; 截图 u5_timeline.png / u5_jump.png

**备注**: 验证期间后端被用户侧重启过 (端口 8720 实例 PID 变化), 未再干扰。

## Resolution 补充 (2026-08-05, 用户实测发现后追加)

**新 bug (cubx 帧详情空白)**: 用户点击 Link Status 帧只显示头部 "帧 #576 | ... | Link Status | 已解密", 无具体数据。
- **根因**: cubx_reader (scapy 路径) 的 `raw_layers` 初始化为 `{}` (cubx_reader.py:361) 且从不填充 — 输出平铺字段, 不生成 tshark 风格层树。packet_detail 直接返回 raw_layers → 前端 MAC/NWK/APS 全空, 只有 frame-meta 头部。
- **修复** (files.py): `_fallback_layers()` — raw_layers 为空时从平铺字段构造简化层树 (wpan/zbee_nwk/"Command Frame: <名>"/Security Header/APS/ZCL), 字段名对齐前端 tlShowDetail 取值; `_fallback_nwk_cmd_tree()` 支持 Link Status 邻居 / Network Status code+target / Leave 标志 / Route Record 中继。展示层兼容, 不改解析器 (P5 边界外)。
- **验证** (8721 实例, 中继入网抓包(1).cubx 4157 包): Link Status 帧 #33 详情完整 — MAC/NWK/Security + Neighbors (5) (0x1885/0x5FDD/0x8A41/0xB75C/0xF67F, 与 L3-5 素材设备一致); Leave 帧 #5 — Rejoin Yes (设备将重新入网)/Remove Children No; 无 console 错误。
- **遗留**: ZDP 明细 (Node Desc 等) cubx 路径仍缺失 (平铺字段无) — 属 P5 字段缺口流; 用户侧 8720 实例需重启生效。

## Resolution 补充 2 (2026-08-05, 用户反馈)

**ZCL 命令级显示**: 时间线类型列对 Data 帧显示实际 ZCL 命令 (如 "Data · Report Attributes"), 不再笼统显示 "Data"。zcl_cmd_name 优先 (双路径生成 cubx:566/tshark:442), aps_cluster_name 兜底; tooltip 显示 ZCL 簇+命令; 过滤仍按 pkt_type=Data (展示增强)。提交 c7bc38f, index.html 版本 v=20260805c。验证: 8720 实例 "Data · Report Attributes" / "Data · ZDP: NWK Addr Req" (playwright)。

**ZCL 按簇修正 (ffa9a52)**: 前端 _tlZclCmd 混合表误标 (0x01 Window Covering 簇 被标 OTA: Query Next Image, 实为 Down/Close)。packet_detail 输出 zcl_cmd_name (zcl_defs 按簇解析), 前端优先用, 混合表兜底; 类型列去 "Data ·" 前缀直接显示命令名。**需重启后端 (packet_detail 新字段)**。

**ZDP 载荷明细 (45c3d56)**: 详情面板 ZDP 层显示命令载荷 — Device Announce (0x13) 短地址+EUI64+能力位 / NWK Addr Req (0x00) / IEEE Addr Req / Desc·EP Req / Mgmt LQI / Addr Resp。cubx_reader 保留 aps_payload_hex (单字段低侵入), files._fallback_zdp_tree 解析 (展示层), 前端 zdpLabels 扩展。能力位解析 (终端/路由器/主电源/RxOnWhenIdle)。验证: NWK Addr Req 帧 #133 IEEE Addr 0xA4C138D92F9D8F49 + Req Type + Start Index。tshark 路径 ZDP 原本完整不受影响。**需重启后端 (cubx_reader 新字段)**。

**各层明细补齐 (0ce9217)**: ① Route Request/Reply — cubx_reader 手动载荷解析 (_parse_route_request/_parse_route_reply, 对齐 tshark cmd.route.*), fallback 构造子树; ② MAC 命令帧/Beacon — wpan 加 cmd_id/src64/dst64/beacon_pan/permit (字段原已有, 补齐展示, 前端名称表 AssocReq/AssocResp/BeaconReq); ③ APS 命令帧 — cmd_id/key_type/remove_target/update_status (TransportKey/Confirm/RemoveDevice 名称表 + key_type: 1=NWK Key 4=TC Link Key)。验证: Route Request 帧 #184 详情完整 (Originator 0xC16C/Path Cost 1/Route ID 82/Options); 单测 route_req/reply/zdp 构造通过; **MAC/APS 命令帧端到端待含此类帧素材 (中继/入网素材均无, 诚实标注)**。**需重启后端**。

**帧号列 + APS Ack 配对跳转 (d541a43, 2026-08-06)**: ① 列表/详情端点补 packet_id (抓包原始帧号; id=过滤后索引与 packet_id 同帧双标识); ② 表格加帧号列; ③ 详情配对行 "点击帧 #X 查看原帧" 可点击 → 定位三态: 本页有→直接定位 (过滤保持) / 过滤内其他页→翻页定位 (过滤保持) / 过滤外→清除过滤重查定位 (提示); 高亮 .hl + 滚动 + 加载详情; ④ 顺带修复: **APS Ack 帧类型列被 ZCL 逻辑误标为簇名** (APS Ack 也带 cluster 且已解密) — 仅 Data 帧走 ZCL 显示。验证: 帧号列 ✓; 配对 id=36→31 定位高亮+详情反向配对 ✓; 过滤外 (APS Ack 62 包) 跳转清除过滤定位 ✓ (playwright)。**需重启后端 (packet_id 字段)**; index 版本 v=20260806a。

**过滤联动 P1+P2 (3b8f143, 2026-08-06)**: P1 邻居表跳转清时间 (topo.js:651 onclick 统一加 topoT0/T1=null, 与 PAN 按钮/cy 节点一致; 修复旧时间窗口残留); P2 时间线时间窗口→拓扑同步 — 旧代码字符串赋 tsStart (NaN) + tsStart/tsEnd 被覆盖 (破坏滑块比例) + 数据未带窗口; 修复: topoTs() 双格式转换, tsStart/tsEnd 恒=抓包范围, init 数据用 UI 窗口 (getTimeWindow), syncWinFromTimeline (档位+滑块+标签)。验证: P1 跳转后时间=抓包范围+搜索 532 包; P2 时间线 10s 窗口→拓扑 winSize=30 + 数据过滤 (1 vs 112 节点)。窗口档位化近似 (10s→30s) 属离散档位固有。
