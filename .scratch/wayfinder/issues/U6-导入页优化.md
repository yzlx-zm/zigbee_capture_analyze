# U6-导入页优化 — UI 工程

**Assignee:** Claude (2026-08-04) — 认领

**What to build:** 导入页 (148 行) 体验优化: 导入流程反馈/密钥面板/校验报告展示。

**Blocked by:** None

**Status:** ready-for-agent

**Type:** task | **HITL/AFK**

**UI 工程背景**: 见 docs/ui_overview.md (UI 工程 wayfinder 拆分地图)

## Resolution (2026-08-04)

**结论**: 三方向全落地 (流程反馈/密钥面板/校验报告) + 真实导入进度条 + 顺带修复 2 个后端 bug (校验报告链路)。22/22 CDP 交互验证通过。

### 补充: 真实导入进度条 (2026-08-04, f37e23b + 1c3d830)

- **后端任务化**: `files.py` 6 个导入端点全部后台线程 — POST 立即返回 `{task_id}`, 新增 `GET /api/import/progress?task_id=` 轮询 (done 时附完整 result 免二次请求); 单任务并发防护; 任务表保留最近 20 条; 阶段基准 上传→Key 同步 10%→tshark 解析 15-40%→MAC 45%→校验逐项 50-95%→完成
- **前端**: `setProg(msg, pct)` 进度条宽度; `doI/doPI` 改 **XMLHttpRequest 上传** (`upload.onprogress` 真实上传百分比 0-10%, 解决大文件 fetch 无上传进度 → 0% 静止); `pollImport` 立即首查 + 300ms + done 标志防重复回调 + 5 分钟超时兜底
- **用户反馈回归修复 (2026-08-05)**: cubx 导入同样卡 0 然后瞬间完成 — 根因 `_run_cubx_import` 中 `parse_cubx()` 是单次黑盒调用, 30MB cubx 解析 ~100s 期间零进度上报。修复: `cubx_reader.parse_cubx` 加可选 `progress_cb(done, total)` (逐包循环每 500 包+末包上报), `_run_cubx_import`/`_run_cubx_local` 传回调映射任务进度 (上传 10-90% / 本地 30-90%)。实测 30MB cubx: 0%→10% (0.8s)→90% 平滑推进 (83 点, ~1.1s/%)→done, 全程 97.5s 不再静止

**验证** (素材: 1-标准入网抓包-2.pcap, 335 包/19 节点, 后端 8720 + Edge headless CDP 驱动):

**产出**:
- `frontend/js/import.js` (重写 147→217 行):
  - **流程反馈**: `#prog` 升级为 spinner + 文案 (`<span class="spin">`);上传中 `#mc.busy` 禁用所有导入控件;失败内联红色错误条替代 `alert()` (含网络错误/后端 error 透传)
  - **密钥面板**: 前端 hex 预校验 (去冒号/空格/连字符/0x 前缀 → 32 位 hex),非法内联报错不弹 alert;添加/删除失败内联显示后端 error;key 点击展开/收起 (17 字符截断 ↔ 32 位全值);命中状态带帧数 `✓ 命中 (N帧)` + 顶部 `N 个 Key 命中` badge;Enter 提交
  - **校验报告**: 删除 import.js 末尾重复渲染 (原 sr() 渲染 + verify 兜底查询双份显示);详情改 `<details>` 可展开差异明细 (不再 300 字符截断)
  - **其他**: 清除数据按钮确认倒计时 (3s 文案, 自动还原)
- `frontend/js/state.js`: `setProg` 升级 (spinner 容器 + busy 类);新增 `setErr`;`doI/doPI` 失败改内联错误 + `d.ok===false` 时不再渲染成功卡片;`sr()` 校验报告结构化 (details 展开/预期-实际/转义),失败响应 (`d.ok===false`) 内联报错
- `frontend/css/app.css`: 新增 `.spin` 动画/`.prog(-err)`/`.key-hex`/`.verify-detail`/`#mc.busy` (注: 此修改随并行 U7 会话提交 e7539a5 入库)
- `backend/api/files.py`: **bug 修复** — `import_pcap` 缺 `global _verify_report`,verify 赋值落入局部变量 → 上传 pcap 响应 verify=None → 前端校验报告缺失 (与 `import_local_pcap` 行为不一致);cubx 路径 verify=None 属设计 (无 tshark 校验)
- `backend/verify.py`: **bug 修复** — `_check_type_distribution` 空包 (0 帧) 时 `actual` 格式化除零 → 整个校验报告被 "校验执行异常" 吞掉;改为 "N/A (0 帧)"

**验证** (素材: 1-标准入网抓包-2.pcap, 335 包/19 节点, 后端 8720 + Edge headless CDP 驱动):
- CDP 交互验证 22/22: 校验报告唯一一份 (alert=1, 原重复 bug 确认);密钥面板 316 行 (315 key+表头);key 展开 17→32→17;非法 hex 内联报错无 alert;添加/删除 key;清除按钮 3s 倒计时+自动还原;busy 禁用+spin;setErr 红色错误条;失败报告 alert-bad + details 展开 + 无 undefined + 无字段项显示 -/-
- 脚本: `.scratch/verification/u6-import/cdp_test.mjs` (可复现);截图 u6_import.png
- 后端 bug 修复实证: 导入响应 verify passed=True, 6 项 checks 全通过;空包 verify 返回 "N/A (0 帧)" 不崩溃

**注**: 本地路径导入 (`lpath/plpath`) 仍用 `prompt()` — 浏览器限制, 保留现状 (U1 已确认)
