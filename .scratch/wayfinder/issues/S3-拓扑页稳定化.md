# S3 — 拓扑页稳定化 (S0 打包前界面稳定化第 3 站)

**What to build:** 打包分发前拓扑页 code-review + bug 排查, P0/P1 清零, 保证工具稳定。
**Blocked by:** None | **Status:** 进行中
**Assignee:** S3-拓扑稳定化-20260827

**来源**: S0 总纲 (2026-08-26), 用户指定下一站 S3 拓扑分析 (2026-08-27)。

## 素材集 (S0 指定)

- 中继入网抓包(1).cubx (链路/父节点实证)
- 群控压测包 (多 PAN / 16 锁)
- 卷帘素材 (RR 中继/直连设备)

## 历史遗留对照清单 (bug 候选, 审查时逐条验证)

- U13: link-history 60s 分段阈值为经验值; A657 判 unknown (素材无证据)
- U14: sleeping 状态未素材实证; offline 角标用粗红边框近似
- U7: device_type 为导入时快照, 改代码需重导素材生效
- 通用: 时间窗过滤联动 / 多 PAN 切换 / 空数据兜底 / 未解密素材行为

## 验收

P0 崩溃/白屏/功能错 (必修) / P1 功能缺陷 (必修) / P2 体验 (记录不阻塞) — P0/P1 清零 + 用户抽验即通过

---

## Resolution

**Assignee:** S3-拓扑稳定化-20260827 | **Status:** 进行中 (修复完成, 复验中)

### 问题清单 (code-review + CDP 实测, 素材: 需求31321_2路开关_入网_1ef9.cubx + 中继入网抓包(1).cubx)

**P1 (必修, 全部已修)**:

| # | 问题 | 证据 | 修复 |
|---|------|------|------|
| 1 | **runLayout 固定列分支 ReferenceError** (`nd` 未定义, 应为 nodeDepth) — 点击 📐 布局两次 (切回固定列) 崩溃, 图卡在力导 | CDP Runtime.exceptionThrown 实测捕获 | topo.js runLayout `nd`→`nodeDepth` |
| 2 | **链路历史端点缺 `return out`** — 恒返回 null → 前端 `d.segments` TypeError → "加载失败"; 有链路证据的节点 (如 0xDC18 直连段) 全部不可用 | curl 返回 null + CDP "加载失败" 实测 | topology.py 补 `return {"aid", "segments"}` (契约 = 对象非裸数组, 前端 `d.segments` 取用) |
| 3 | **链路历史打开即错**: histAid=null → 请求 aid=null → 422 → 假象 "0 段链路证据" | CDP Network 请求实测 | topo.js 打开面板时初始化 histAid=sel.value |
| 4 | **route 边 tooltip 显示 "路径 #NaN 第NaN跳"** — U13 时刻游标重构后 route 边来自 link_snapshots 分段, 不再带 path_idx/hop | 代码证据 (cyEdges 构建段) | topo.js edge tooltip 加 route 专属分支 (路由路径·时刻链路) |
| 5 | **路径行 hover 图上高亮失效** — highlightPathOnGraph 按 path_idx 匹配, route 边无 path_idx → 永不命中 (U13 重构回归) | 代码证据 | topo.js 改为按路径链节点对匹配 (与 hist-seg 点击高亮同逻辑) |

**P2 (已修)**:

| # | 问题 | 修复 |
|---|------|------|
| 6 | getWinSize/getTimeWindow/updateTimeScale/applyTimeFilter/showPreviewMask/hidePreviewMask/stepWindow/setWindowFromSize 死代码 — twin-size 元素已删, 任何调用即 TypeError | topo.js 删除 8 个死函数 |
| 7 | runLayout 引用 renderGraph 局部变量 userZoom/userPan → 布局切换缩放状态丢失 (undefined!=null 恒 false) | 删除失效行 (切换布局后 fit 全图) |
| 8 | trst 重置不清 S.topoT0/T1 → 残留时间窗影响后续跳转时间线带旧窗 | trst 补 S.topoT0/T1=null |
| 9 | 小网络 (<10 节点) 默认力导但按钮显示 "▦ 固定列" — 显示与实际布局不一致 | renderGraph IIFE 按 curLayout 设置文本 |
| 10 | _all_link_segments 无 PAN 过滤 — 多 PAN 聚合抓包 RR/poll 证据串网 (link_history + link_snapshots 双处) | topology.py 加 pan_int 参数, 两调用点传入 |
| 11 | events 端点每请求全量重算 _node_stats/_behavior_map/_parent_map/_all_link_segments (O(full)×4) — 大包滑块卡顿 | events 端点整体缓存 (键=包数/full数/PAN/t0/t1, 同窗命中实测 0.26s→0.20s, 大包差异显著) |

**脚本断言澄清 (非功能 bug, 如实记录)**: tlay 断言首击方向写反 (4 节点素材默认力导, 首击=切固定列); 素材导入轮询过早读到旧 status。

### 素材实证 (中继入网抓包(1).cubx, 8435 帧/122 节点)

- 链路历史: 838D = 2 段 (poll 父段 0x1885 + RR 路径段 →0x1885→0x0000), **与 U13 验收记录完全一致**
- 行为状态: 838D end_device/rejoining/poll=0.245s/AKLOCK-C6 (U14 实证复现); 8A41 offline; 1885 router/rejoining
- 时刻游标: 拖动重渲染正常, 0 异常

### 遗留对照 (S0 清单, 诚实标注)

- U13 60s 分段阈值: 本素材分段合理 (poll 父段 0.5s 内连续) — 阈值待更多素材校准
- U14 sleeping 状态: 素材全程 poll, 无"终端窗内无 poll"窗口 — 逻辑按 ticket 口径宁缺勿滥
- A657 unknown: 素材无证据 — 保持诚实标注
- 大包性能: events 缓存已加, 群控包 (10.8 万帧) 终验可选 (待用户素材现场)
