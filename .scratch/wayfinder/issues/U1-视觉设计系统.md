# U1-视觉设计系统 — UI 工程

**Assignee:** Claude (2026-08-04) — 认领

**What to build:** 横切: 建立视觉设计系统 (颜色/间距/组件类/状态色规范), 消除 inline 样式与 61 个零散 CSS 类, 统一为可复用组件类。所有页面受益。

**Blocked by:** None

**Status:** ready-for-agent

**Type:** task | **HITL/AFK**

**UI 工程背景**: 见 docs/ui_overview.md (UI 工程 wayfinder 拆分地图)

## Resolution (2026-08-04)

**结论**: 全量落地 — 视觉设计系统建成立, CSS 全部抽取 + token 化 + JS inline 样式清零 (仅数据驱动动态色保留)。

**产出**:
- `frontend/css/app.css` (重写, ~490 行): 13 组设计 token (`--c-*` 颜色/`--sp-*` 间距/`--fs-*` 字号/`--r-*` 圆角/`--font-mono`), 状态色体系 (success/warning/danger/info 各带浅背景/边框), 组件类 (card 5 变体/btn 6 变体/alert 4 变体/badge 4 变体/legend/tabs/toolbar/timebar/path 面板), 页面专属类 (l1-card/diag-*/tree/bp-tab/tl-bar 等), 工具类 (.row/.grow/.mono/.t-10~13/.w-45~180/.text-*/.hidden 等)
- `frontend/index.html` 109→43 行: 内嵌 `<style>` 全部移除改引 `css/app.css`; 路由层统一 `#mc.mc-flat` 切换 (拓扑页无内边距, 其他页 16px)
- 6 个 JS 模块: inline 样式清零 (topo.js 40 处 / timeline.js 25 处 / import.js 25 处 / diag.js / nodes.js / state.js)
- 补定义此前"使用但未定义"的孤儿类: `.btn-s` `.imp-tab(.on)` `.badge` `.tl-row` `.diag-conclusion` `.pan-tl-btn`

**关键决策**:
- 命名保留现状 (`.btn-p/.btn-o/.btn-r` 而非孤儿 app.css 的 `.btn-primary`) — 降低回归风险, 孤儿文件被重写吸收
- 动态数据色 (链路成本色/协议层色/路径当前色) 保留 inline — 数据驱动非样式重复, 已注释
- 显隐控制统一 `.hidden` 类 (非 !important, 兼容 JS `style.display` 控制)
- 仅浅色主题, token 预留深色位

**验证** (素材: 1-标准入网抓包-2.cubx, 340 包/27 节点):
- node --check 全部 7 个 JS ✅; 类使用 vs 定义核对 ✅
- Edge headless 渲染 6 路由全部出 DOM ✅; diag L1 卡片 4 张 + v-ok/v-bad/v-warn ✅; nodes 27 行 ✅; topo 统计/PAN/图例/cy 容器 ✅; tl 空态 ✅; diag 无事件分支 ✅
- 截图: `.scratch/verification/u1-design-system/` (import/topo/diag 三页, 非空白)
- 后端 8720 端口验证: 导入/静态资源 200 ✅
