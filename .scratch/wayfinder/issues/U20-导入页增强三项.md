# U20 — 导入页增强三项 (Ubiqua key 同步扩展 / 导入选 PAN / 一次拆多段)

**Assignee:** U20 实现窗口 (2026-09-18 ~ 09-20)
**Status:** done (三项独立实现 + 独立提交 + 独立验证)

**What to build:** 三个独立小项: H 密钥同步扩展 (含 Link Key, 解密率关键) / I 导入时按 PAN 过滤
/ J 一次拆多段子包。

**Blocked by:** H 依赖 U19 阶段一的 key 接口验证结论 | **Type:** task | **UI 反馈优化**

**来源**: 2026-09-18 头脑风暴 v3 (用户确认 G/H/I/J 四项)。

---

## H — 密钥同步扩展 (解密率关键项)

### 现状与缺口 (总控已查)
- `_sync_ubiqua_keys` (files.py:252) **只同步 Network Key** (`client.get_network_keys()` →
  `key_store.merge_from_ubiqua`), 且**静默** (成功/失败用户无感)
- **实际解密瓶颈是设备 Link Key**: 项目实证 (P4) 中继包 99.98% 安全帧为 key_type=0
  (设备唯一 TC link key), 58.5% 帧解密失败 = 缺 link key **非 bug**
- Ubiqua 的 `Keys` 表通常同时存 Network Key 与 Link Key; `ubiqua_api.list_keys()` 可读全部

### 决策
- 同步范围: **全部 key 类型** (Network + Link), 按 hex 去重合并 (沿用 key_store 机制)
- 时机: 导入前自动 (现行为) + 提供**手动"刷新密钥"入口**
- **可见性**: 同步结果展示 (如 `从 Ubiqua 同步 +3 个密钥 (1 Network / 2 Link) · 总 306`)
- 未连接 Ubiqua → 静默跳过 (不阻断导入), 但手动入口提示"Ubiqua 未运行"

### 验证
- 同步后解密率提升可量化 (同素材前后对比, 记录实际数字)
- key 类型分类正确 (Network/Link 分别计数)
- 回归: 无 Ubiqua 时行为不变

---

## I — 导入时按 PAN 过滤 (数据纯净度)

### 现状与缺口
- 预扫 (`cubx_splitter.prescan_cubx`) 返回 total_frames/ts_first/ts_last/duration/histogram/
  channels/lqi/rssi — **无 PAN 分布**
- 多网络混杂包常见 (中继/群控包); U13 曾因多 PAN 混杂出过 bug → 数据纯净有价值

### 决策
- 预扫增加 **PAN 分布统计** — 实现方式: 轻量扫描 (只读 Raw 前几字节取 MAC FCF+PAN,
  **不做完整协议解析**, 保持秒级)
- 预扫面板列出 `PAN 0x580C: 2174 帧 / PAN 0x1234: 890 帧 …` → 用户选择目标 PAN
- 导入/拆分时只保留选中 PAN 的帧 (未选 = 全量, 保持现行为)
- 多 PAN 时默认建议选择帧数最多的 (不自动应用, 需用户确认)

### 验证
- PAN 分布与解析后实际统计对账一致 (同一素材)
- 选 PAN 后导入: 帧数 = 该 PAN 帧数; 未选 = 全量 (回归)
- 单 PAN 素材: 面板不显示选择器 (无干扰)

---

## J — 一次拆多段 (批量拆子包)

### 现状
- 拆分面板一次拆一个窗口 (U18 加了 ◀▶ 分段推进, 但仍是逐段)

### 决策
- 面板加"按段长批量拆": 输入段长 (分钟) + 范围 (当前窗口) → 一次拆出 N 个子包
- 结果进现有子包清单 (N 条, 下载/导入按钮不变)
- 边界: 末段不足一段长 → 按实际截断 (或丢弃, 实现时定, 需提示);
  段数上限保护 (如 ≤20 段, 防误操作产生海量文件) + 超额提示
- 性能: 优先**单次扫描拆多段** (若现有 `split_cubx` 是单窗, 评估是否需要扩展为多窗一次扫描;
  简单实现=循环调用时, 如实记录耗时)

### 验证
- 60 分钟素材拆 5 分钟段 → 12 个子包, 帧数总和 = 源窗帧数 (无重叠无遗漏)
- 子包清单 N 条, 下载/导入可用
- 段数上限与越界提示正常

---

## 通用要求

- 三项均改 `frontend/js/import.js` + `backend/api/files.py` (+ `cubx_splitter.py` for I/J)
  → **版本号递增** (index.html/app.js, 项目铁律)
- 回归: 现有导入/拆分/子包流程 + S1 修复项 + U18 时间窗体验 全部不受影响
- 每项独立验证 + 独立提交 (便于单项回退)
- 收尾: ticket Resolution (三项分别记录) + map.md 条目 + commit + push

---

## Resolution (2026-09-20, U20 实现窗口)

### 开工核对: 两个前置缺口 (如实记录, 未绕行)

1. **U18 未实现** — 面板仍是 `应用` 按钮 + 无 ◀▶ 分段 + 无 localStorage 持久化
   (U18 ticket 无 Assignee/Resolution)。→ J 项按**现状面板**做加法: 独立"批量拆"
   控件组, 未触碰 U18 的目标区域; U18 落地时与本次改动无同区冲突。
2. **U19 未实现 + Ubiqua 本机未运行** (19501 实测 `http=000`) → H 依赖的
   `list_keys()` 类型验证**无法在真实 Ubiqua 上完成**。改用两条替代证据, 并标注
   真实 Ubiqua 复验待用户环境:
   - **Ubiqua 自身数据模型实证**: 55 个素材 cubx 的 `Keys` 表 schema =
     `[Id, Key, Type]`, Type ∈ {NetworkKey, LinkKey} (全网 196 NetworkKey /
     92 LinkKey, 多数包同时含两类) → 全类型同步有据可依
   - **本地 mock 服务**复现其 XML 契约 (`urn:ubilogix:services`), 密钥源取真实素材
     Keys 表 (非编造), 走完整链路验证

---

### H — 密钥同步扩展全类型 (提交 d4f1f1a)

**实现**: `ubiqua_api.list_typed_keys` (全类型/去重/类型缺省 Unknown) +
`key_store.merge_from_ubiqua` 类型化入参 (返回 `added_by_type`/`by_type`, 标签
`ubiqua_net_*`/`ubiqua_link_*`) + `POST /api/keys/refresh-ubiqua` + 前端密钥面板
「🔄 从 Ubiqua 刷新密钥」按钮/类型徽章/结果文案 + 导入结果卡同步行。

**实测数字**:
| 验证 | 结果 |
|---|---|
| 同步链路 (mock, 真实素材 Keys 表) | **17/17** (解析/分类计数/去重/幂等/不可达/落盘) |
| 解密率前后对比 (pcap, 同素材) | test2 `0/9303 = 0.0%` → `1029/9303 = 11.1%`; <br>标准入网 `33/335 = 9.9%` → `39/335 = 11.6%` |
| LinkKey 覆盖度实验 | 追加全部素材 29 个 LinkKey → 中继包 `1729/4150 = 41.7%` **无变化** |
| CDP | mock 6/6 + Ubiqua 未运行 3/3 |

**诚实标注**:
- LinkKey 那条**未测出正收益**: 中继素材的设备 link key 不在任何素材 Keys 表内
  (密钥库没有就是解不开) → H 的价值取决于 Ubiqua 实时 Keys 表的实际覆盖, 无法用
  现有素材证明; 全类型同步本身已闭环 (类型计数/落盘/幂等实测)
- 真实 Ubiqua (192.168.x 抓包机) 的 `/keys` 实际 XML 未验证 → 待用户环境复验

**顺带修复 (实测发现)**: `zigbee_pc_keys` 预设 key 重复读出 —
`write_all_keys` 会把预设写进文件, 读取时又当自定义条目 + `_with_presets` 再加一条
→ 用户实机文件 **356 行 / 321 个 hex, 预设重复 36 次** (面板多一行、统计多算一份)。
修复: 读取时跳过预设行, 下次写入自愈。⚠️ **未擅自改写用户文件**: 磁盘上的 35 行
冗余仍存在 (Wireshark 语义无影响), 修复后不再累积, 下次同步/加 key 时自动收敛到
321 行。

---

### I — 导入/拆分按 PAN 过滤 (提交 a2a7584)

**实现**: 轻量 MAC 头 PAN 提取 `mac_pan_ids` (不跑协议栈, 源码级对齐 scapy
dot15d4: 帧类型/寻址模式/信标 Superframe/FCS 规则) + `zigbee_relevant`
(排除解析器不保留的异协议帧) + 预扫 `pans[]` (0.11s / 10.8 万帧) + 拆分与导入的
`pan` 过滤 (同口径) + 面板 PAN 下拉 (帧数降序, 最多 20 项, 最多者标"建议",
默认"全部"不自动应用, 单 PAN 素材不显示) + 结果卡「已按 PAN 0xXXXX 过滤」。

**对账 (预扫轻量 vs 权威解析逐帧, `i_pan_reconcile.py`)**:
| 素材 | 帧数 | 轻量合计 vs 权威合计 | 主 PAN |
|---|---|---|---|
| 群控压测 | 108474 | 54409 / 54403 | `0xA736` 52047 **一致**, `0x7DD1` **一致** |
| 中继入网(1) | 8626 | — | `0x580C` 2176 **一致** |
| 标准入网 | 887 | 464 / 467 | `0xCB43`/`0x17FC` **一致**, `0x5408` 差 2 |

残余 ±1~2 帧/PAN (≤0.25%), 全部是 scapy 同样拒绝寻址 (保留寻址模式/超短信标) 或
寻址口径不同的畸形帧 → **不进数据**。已在面板说明与 ticket 标注, 不虚报"完全一致"。

**实测**: 中继包 未选 PAN → 8435 帧 (回归不变) / 选 `0x580C` → 2176 帧 12 节点
**解密率 41.7% → 100%** (该网络的 key 齐全; 混入的其他 PAN 网络 key 未知——
这正是数据纯净度的价值) / 选 `0xC3D3` → 1912 帧 71 节点 (解密 0%, 未知网络);
拆分 全窗+PAN 2176 帧、半窗+PAN 1095 帧, 子包**仅含该 PAN** (辅助表逐表一致,
sqlite_sequence 对齐)。CDP: 多 PAN 9/9 + 单 PAN 3/3 + 选 PAN 整包导入 5/5。

**过程修复**: 首版 `zigbee_relevant` 缺失时, 中继包会出现一个"可选中但导入必为空"
的 PAN (`0x47E4` 45 帧扩展地址垃圾帧) → 补相关性判定后消失。

---

### J — 一次拆多段 (提交 f1cb3c7)

**实现**: `split_cubx_multi`(单次扫描多段, 半开区间无重叠, 末段截断, 段数上限 20,
PAN 可组合) + `POST /cubx/split-batch` (段数预检 400) + 面板"批量拆"控件组
(段长 + 实时段数预览 + 超额禁用) + `runSplit` 抽公共 (单窗拆分行为不变)。

**实测**:
- 群控 4.9MB / 108474 帧 / 43.5 分钟 → **5 分钟段拆 9 段, 0.5s** (单次扫描,
  无循环调用开销); **子包帧数和 = 108474 = 源窗帧数 (无重叠无遗漏)**, 各段窗口
  边界与帧数逐段核对一致; 末段 3.53 分钟 → `truncated_last` 标注
- 段长 1 分钟 (44 段) → 400 拦截, 文案给出"请增大段长或缩小范围"
- PAN + 分段: 10 分钟段拆 5 段 = 52047 帧 (该 PAN 全量), 子包无非该 PAN 帧
- CDP: 批量拆 9/9 (UI 帧数合计 108474 与源一致) + S1 兼容复验 6/6

**顺带修复 (J 验证暴露的两个真 bug)**:
1. **range 控件 step=1 量化窗口** (先于本次存在): 滑块赋值被量化到 `min+k*1`,
   群控实测末端 `1785745240.809276` → `...240.01891` → **全窗选择丢末段 0.79s /
   27 帧** (S1 曾修"末帧必丢"的闭区间问题, 但量化这层没被发现) → 改 `step="any"`
   + 边界吸附 `csWindow()`; 修复后 UI 与源帧数完全一致
2. **首版 J 自身的块级作用域 bug**: `updSegHint` 等声明在 `if(csPanel){}` 块内 →
   ES module 严格模式下块级函数不外泄 → `updWin` 抛 ReferenceError → **面板
   `dataset` 未写入 (path/fname 全丢), 拆分与整包导入报"路径不存在: undefined"**;
   已提到 reg 作用域并 CDP 异常捕获定位复验

---

### 回归与收尾

- **回归全绿**: P1 15/15 · zcl_fcf 12/12 · p2 4/4 · parser_verify 12/12
- **CDP 清单**: H mock 6/6 + down 3/3; I 多 PAN 9/9 + 单 PAN 3/3 + PAN 整包导入 5/5
  (I 三脚本在 J 的修复后**重跑全绿**); J 批量拆 9/9 + S1 兼容 6/6
- 版本号: index.html/app.js `20260918a`(H) → `b`(I) → `c`(J), import.js 同步递增
- 验证脚本/截图: `.scratch/verification/u20-import/`

### ⚠️ 2026-09-20 用户反馈: J 项"批量拆"已撤回

**用户原话**: "批量拆: 分钟/段 按段长批量拆 段长需 > 0 有点莫名其妙。功能点意义不明,
我感觉要去掉"。

**处置 (撤回 J 功能本体, 保留其暴露的 bug 修复)**:
- 删除: 面板"批量拆"控件组 + `POST /cubx/split-batch` + `split_cubx_multi` +
  `MAX_SEGMENTS` + J 的验证脚本/截图 (commit 见下)
- 保留: ①J 验证暴露的**窗口量化修复** (`step="any"` + `csWindow()` 边界吸附 —
  全窗丢末段 27 帧, 影响既有"拆分子包"流程) ②`runSplit` 公共运行器 (单窗拆分)
  ③I 的 PAN 过滤 (用户未提, 且价值明确)
- 可回溯: J 的完整实现在 git 历史 `f1cb3c7` (含 9 段/0.5s 单次扫描实现), 如后续
  (如 U18 分段交互) 需要可直接取回

**"段长需 > 0" 复现结论 (如实标注)**: 干净会话下该行显示为
`批量拆: [5] 分钟/段 [按段长批量拆] 将拆为 N 段`; 该提示文案只在输入框值被判为
空/非法时出现 (手改输入框, 或旧提示残留)。**未能复现用户所见的稳定状态**, 不编造原因;
控件已移除, 该问题随功能一并消失。

### 收尾状态 (2026-09-20)

- 4 次提交已完成 (H d4f1f1a / I a2a7584 / J f1cb3c7 / 文档 20a1f69)
- ⚠️ **push 未完成**: github.com:443 不可达 (代理 127.0.0.1:7897 未监听, 直连超时)
  → 本地领先 origin/main **7 个提交** (含前序会话 36daf3a/f91dca6/5a46219 三个 docs 提交,
  说明推送在本会话之前就已中断); 网络恢复后需 `git push` 补推
- 环境善后: 用户 zigbee_pc_keys 与开工备份**逐字节一致** (356 行, 未擅改);
  CDP 浏览器已关; 后端 (8720) 保留运行且数据已清空 (可直接试新功能)

### 遗留 (诚实标注)

1. **真实 Ubiqua 复验** (H): mock 之外需用户在抓包机上复验 `/keys` 实际返回
   (类型字段名/是否含 LinkKey)
2. **单 PAN + >1MB 素材**: 现实素材无此组合 → 用**合成件** (水沁素材复制 30 份,
   1.1MB) 验证"单 PAN 不显示选择器"分支; 真实大包单 PAN 场景未实测
3. **精确时间输入的末分钟语义**: 输入 16:20 结束 → 截到 16:20:00 (丢掉该分钟
   剩余 40.8s)。S1 设计为分钟粒度, 未擅改; 是否改为"含该分钟"待用户裁定
4. **S1 复验脚本已失效** (`s1-import/s1_verify2.mjs`): 引用 CSV tab (`.imp-tab`,
   S1 自审已删) 与临时 fixture (`/_s1-*.cubx` 已不存在) → 不可直接运行; 本次以
   `cdp_j_s1_compat.mjs` 覆盖受影响的同等行为 (时间输入/窗口/子包按钮)
5. 预扫 PAN 计数与过滤后帧数存在 ≤0.25% 畸形帧差 (见 I 对账表)
