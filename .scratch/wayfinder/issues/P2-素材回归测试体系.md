# P2-素材回归测试体系 — 解析器工程

**Assignee:** Claude (2026-08-10) — 认领
**What to build:** 素材回归测试体系: 已知素材 (健康 1-标准入网抓包-2 / 故障 中继入网抓包 838D / DA13) 字段快照 + 自动回归脚本。改解析器必跑, 防回归。

**Blocked by:** None

**Status:** ✅ 完成 (2026-08-10)

**Type:** task | **HITL/AFK**

**解析器工程背景**: 见 docs/parser_overview.md (解析器工程 wayfinder 拆分地图)

## 方案 (2026-08-10)

**快照粒度** (每素材 .scratch/verification/p2-snapshots/{name}.json):
1. 统计层: 总物理帧 / 解析保留帧 / pkt_type 分布 / 关键检测字段计数
   (Leave rejoin/request、NWK 命令分布、0x0B/0x0C、APS 命令分布、节点数)
2. 指纹层: 每帧指纹 (packet_id + 关键字段) 的 SHA-256 聚合 — 逐帧差异可检测
3. 解析器签名: 触发快照的解析器版本 (git commit)

**回归脚本** p2_regression.py (入库):
- 默认: 解析全部素材 → 与快照对比 → PASS/FAIL + 差异定位
- --update: 生成/更新快照 (首次或解析器有意变更后)
- --slow: 含大包 (29MB, ~30s) — 默认快集 (中继/标准/DA13/群控)

**素材清单**: 健康 1-标准入网抓包-2 / 故障 中继入网抓包(1) (838D) / DA13-2 /
群控压测问题包; --slow 含 29MB 大包

**顺带**: test_parser_verify 3 个既有失败根因排查 (tshark 权威对比 3.92%)

## Resolution (2026-08-10)

**产出**:
1. `p2_regression.py` (入库): 素材快照 + 自动回归 — 默认快集 (标准入网/中继 838D/
   DA13-2/群控) + --slow 大包 (29MB); --update 生成快照; 快照含统计层 (帧数/
   pkt_type 分布/Leave/NWK 命令/0x0B/APS 命令/节点数) + 每帧指纹 SHA-256 聚合 +
   **解析器专属 commit** (git log 限 backend/cubx_reader.py, tshark.py, zcl_defs.py —
   UI 提交不触发失效)
2. 初始快照 4 份: `.scratch/verification/p2-snapshots/` (解析器 commit 5c2ae08)

**顺带修复 — test_parser_verify 3 个既有失败根因 (P6 校验器 bug)**:
- 根因: `parser_verify.py` 权威对比 `int(auth_seq, 16)` 把 tshark 4.6 输出的
  **十进制** seqno ('198') 按十六进制解析 → 408≠198 → 基准 255 帧仅 10 匹配
  (3.92%)。tshark.py `_num` 已处理过同源问题 (seq 类字段十进制/地址类 0x 前缀),
  parser_verify 漏了。
- 修复: `int(x, 16) if 0x 前缀 else int(x, 10)` (兼容两种格式)
- 验证: test_parser_verify **9/3 → 12/12 全过** (基准权威匹配恢复 100%)

**回归**: p2_regression 4/4 + p1_regression 15/15 + test_parser_verify 12/12

**备注**: 快照生成时 parser_commit=5c2ae08; 后续改解析器 3 文件后 --update 刷新
快照 (有意变更) 或对比差异 (无意回归)。

**待办**: --slow 大包快照 (29MB ~30s, 需要时 --update --slow 补)
