# U16 — 时间线看包体验优化 (grilling 对齐产出, 2026-08-25)

**What to build:** 时间线页 7 项优化: 过滤快速填写 / 未解密默认隐藏 / 类型列改摘要 / 路径列
替代安全状态列 / APS 计数列 / 层级颜色 / 事务链关联 + 字段点选过滤。

**Blocked by:** None

**Status:** done

**Assignee:** U16 实现窗口 (2026-08-25)

**Type:** task | **AFK**

**来源**: 2026-08-25 总控窗口 grilling 对齐 (用户逐条核对 7 点 + 发散 v2 选 A/B)。

## 对齐决策 (用户确认, 不可更改)

| # | 需求 | 决策 |
|---|------|------|
| 1 | 过滤快速填写 | 详情面板 PAN/源/目标字段点选 → 填入过滤框 (与 #B 合并为通用字段点选) |
| 2 | 未解密包过滤 | **默认隐藏未解密包** + 过滤行开关可切换显示 |
| 3 | 类型列改摘要 | 类型列 → **包信息简述** (如 `ZCL On/Off C→S 0x0006` / `Leave rejoin=1`), 复用 AI 导出摘要思路 |
| 4 | 路径列 | **安全/状态两列替换为"路径"列**: 下行 source route 显示 `→中继A→中继B`, 无路径显示 `—` (安全信息详情面板已有, 列表不占列) |
| 5 | APS 计数列 | 列表加 "APS Ctr" 列 (aps_counter, 分析消息回复情况) |
| 6 | 层级颜色 | **类型列着色**: ZCL 绿 / APS 紫 / NWK 蓝 / MAC (DataReq 红, 其他灰) — 文字+底色, 行其余列不染 |
| 7a | 事务链关联 (发散 A) | 点请求帧 → 同 APS counter 的 Ack/响应帧**高亮** + 一键跳转 (复用 L3-1 aps_pairing 能力) |
| 7b | 字段点选过滤 (发散 B) | 详情面板任何字段值 (Src/Dst/Cluster/命令/端点) 点击 → 加入过滤条件 |

**未选**: 关键帧行首图标 / 导出 CSV / 帧密度条 / 相对时间 (用户弃, 不列入)

## 现状盘点 (总控已查, 勿重复)

- 表格列: 帧号|时间|类型|NWK Src|NWK Dst|安全|状态 (timeline.js 47-49)
- 过滤: PAN 框 + 节点框 + 时间下拉 + 类型下拉 + 查看按钮
- 数据: /api/packets 返回 ts/ch/pkt_type/security/status/zcl_cmd_name/decrypted;
  aps_counter 已在详情 (files.py:1023); **列表响应未含 aps_counter/nwk_relays/summary**
- 路径数据: nwk_relays (下行 source route, cubx_reader 已有); 上行路径需关联 Route Record
  (U13 已建 RR 下一跳推断, 可复用 topology.py 证据)
- 摘要: export_ai_dataset.py packet_summary 已有完整语义摘要 (403 行起), 可复用精简
- 事务配对: backend/aps_pairing.py (L3-1) 已有 counter 配对能力

## 实现要点 (总控设计)

### 1. 后端 — backend/api/files.py /api/packets 列表响应扩展
- 每条加: `aps_counter` / `nwk_relays` (下行) / `summary` (简述) / `layer` (zcl/aps/nwk/mac 层级)
- summary 生成: 精简版语义 (pkt_type + zcl 命令名 + 方向 + 关键标志 Leave rejoin/NS 码),
  参考 export_ai_dataset.packet_summary 简化, 不引入完整 AI 摘要的复杂度
- layer 判定: zcl_cmd_id 非空→zcl; aps_cmd_id/aps_cluster→aps; nwk 命令→nwk; mac 帧→mac
- 上行路径: 可选增强 — 通过 (src,dst) + 时间窗关联 U13 RR 证据; 首版可只做下行
  nwk_relays + 上行关联 RR (U13 已有 route_paths 数据源), 实现时评估复杂度
- decrypted 过滤参数: `hide_undecrypted=1` (默认 0, 前端默认传 1 并带开关)

### 2. 前端 — frontend/js/timeline.js
- **过滤行**: 加"🔒 未解密"开关 (默认开 = 隐藏未解密); 类型下拉保留
- **表格列调整**: 帧号|时间|摘要(原类型)|路径(原安全/状态)|NWK Src|NWK Dst|APS Ctr
- **摘要列**: 渲染 summary (短文本, 过长截断 hover 全显)
- **路径列**: nwk_relays 非空 → `→0xXXXX→0xYYYY`; 空 → `—`
- **层级着色**: summary 列文字+底色按 layer (zcl=绿 / aps=紫 / nwk=蓝 / mac=DataReq 红 else 灰)
- **字段点选过滤 (#1+#B)**: 详情面板字段值 (PAN/Src/Dst/Cluster/命令/端点) 渲染为可点,
  点击 → 填入对应过滤框 (PAN→tl-pan, 节点→tl-node, 类型→tl-type) + 自动"查看"
- **事务链 (#A)**: 详情面板请求帧显示"同事务帧"链接 (aps_pairing 结果, 详情端点已有
  aps_ack_pair) → 点击跳转对应帧 (复用 tlPendingJump 机制) + 表格高亮该事务帧
- 版本号递增 v=YYYYMMDD

### 3. 不动项
- 时间下拉/分页/类型下拉动态化/PAN-节点联动 (U5) 逻辑保留

## 验证标准

1. 未解密: 导入含密文包素材 → 默认列表无未解密帧, 开关关闭后显示
2. 摘要列: ZCL 命令帧显示 `ZCL On/Off C→S`; Leave 帧显示 rejoin 标志
3. 路径列: 中继素材下行 source route 帧显示中继列表; 无路径帧显示 —
4. APS 计数: 列显示 counter, 请求/ack 帧可肉眼对应
5. 层级颜色: 各类帧 summary 列着色正确 (CDP 截图)
6. 字段点选: 详情点 Cluster → 过滤框填入 + 自动查看
7. 事务链: 点请求帧 → 同 counter ack 帧高亮 + 跳转 (CDP)
8. 回归: 现有时间线功能 (跳转联动/详情面板/U15 载荷解析) 不崩; P1/P2 不变

## 风险

- summary 生成性能: 列表 200 条/页, 后端逐条生成可接受; 大包全量? summary 只对返回页
  生成 (在列表端点内, 非全量)
- 上行路径关联复杂度: 首版只做下行 nwk_relays, 上行 RR 关联若复杂标"待增强"
- 事务链高亮: 跨页事务帧需处理 (不在当前页时跳转翻页后高亮)
- 与 U13/U14 无冲突 (timeline.js 独立); U15 刚动过 timeline.js 详情区 — 版本号注意

## Resolution (2026-08-25 U16 实现窗口)

**全部完成并验收通过 (提交 1048e70, 推送 origin/main)。**

### 7 项落地
1. **PAN/地址点选** (#1+#7b 合并): 详情面板 PAN/短地址值可点 → 填过滤框 + 自动查看; Cluster/命令/端点点选用户裁定不做
2. **未解密开关**: 默认隐藏 + 过滤行开关切换自动重查 (hide_undecrypted 参数)
3. **摘要列**: 类型列 → 后端 summary (ZCL 命令/Leave 标志/NS 码等, 简化自 export_ai_dataset.packet_summary); 用户反馈后去掉簇 ID/方向
4. **路径列**: 安全/状态两列 → 路径列; 完整路径 起点→中继→终点 + 点击展开; **上行路径补全** (下行 source-route 反转, RR 证据实证不足: 31 帧仅 3 帧 relays 非空)
5. **APS Ctr 列** (列表与详情交叉验证一致)
6. **层级着色**: ZCL绿/APS紫/NWK蓝/DataReq红/其他灰, 仅染摘要列
7a. **事务链**: ZCL 命令帧 → ack + 同 tsn 响应帧, 点击跳转高亮; ⚠️ 收紧修正: 首版误配 (APS 命令帧 cluster=None 降级 33 帧全列) → 仅 ZCL 帧建链 + 仅同 tsn 匹配; 展示折叠 (前5+展开)
7b. 用户弃 (PAN/地址点选已覆盖)

### 用户驱动的附加改动
- **页面改名 时间线→报文** (导航/标题/跳转/锁定提示)
- **全量化** (方案 A): _packets=全量帧 4158→8435 (poll/Beacon 入列), 未解密语义修正 (仅 NWK 安全未解密隐藏; 明文 MAC 帧不隐藏)
- **MAC Ack 支持** (方案 B, 用户裁定): cubx/tshark 双路径解析 ft=2 (seq+pending), 8626=8435+104 reserved+87 无信息帧; 锁侧 ACK 分析数据源完整化
- **进页面自动加载全量** (用户反馈空页面)
- **DataReq 命名修正**: scapy 源码级 (dot15d4.py:327): 4=DataReq, 3=DisassocNotify; 加密 MAC 命令帧垃圾 cmd_id 不显示假名
- **🔒 仅限 NWK 安全未解密** (自查修正: 明文 MAC 帧误标)

### 验证
- P1 回归 15/15; P2 快照刷新 4/4 (解析器级改动已入库)
- CDP 9 脚本 74/74 (u16-timeline/ 目录, 素材 中继入网抓包(1).cubx)
- 性能: 事务构建 8435 帧 0.59s (惰性缓存)

### 遗留/待增强
- 上行路径用下行反转 (对称假设), RR 证据不足; 若需严格上行路径需 RR 素材
- pcap 路径 MAC Ack 解析已实现未实测 (素材全 cubx)
