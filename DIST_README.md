# Zigbee Capture Analyzer — 使用说明 (分发版)

Zigbee 抓包离线分析工具：导入 .cubx / .pcap 抓包 → 自动诊断网络问题 →
拓扑/报文/节点/AI 侧边栏多页面分析。

## 快速开始

1. 解压 `ZigbeeAnalyzer-<版本>.zip` 到任意目录（**建议英文路径**，中文路径也可用，见下方说明）
2. 双击 `ZigbeeAnalyzer.exe`
3. 自动打开浏览器 → 进入工具（首次启动稍慢，正在准备环境）

> **退出工具**：关闭黑色控制台窗口即可（或直接关浏览器页）。
> 再次双击 exe 会打开新浏览器页连接到已运行实例；若提示"工具已在运行"，
> 说明之前未正确退出，请先关闭旧窗口。

## 数据目录（重要）

| 内容 | 位置 | 更新时 |
|------|------|--------|
| 程序文件（exe/依赖/前端页面/version.json） | 解压目录 | **覆盖替换** |
| AI API key 配置（敏感） | `%APPDATA%\zigbee-analyzer\ai_config.json` | **不碰，自动保留** |
| 运行日志 | `%APPDATA%\zigbee-analyzer\logs\app.log` | 自动轮转（2MB×3） |
| 解密密钥（zigbee_pc_keys） | `%APPDATA%\Wireshark\zigbee_pc_keys`（系统级，随 Wireshark） | 自动读取 |

**数据分层原则**：升级/覆盖程序文件不会丢失你的 AI key 配置和日志。
删除整个解压目录即完成卸载（`%APPDATA%\zigbee-analyzer\` 可一并删除）。

## 更新方法（手动覆盖）

1. 下载新版 `ZigbeeAnalyzer-<新版本>.zip`
2. **先关闭正在运行的工具**（关控制台窗口）
3. 解压新版 zip，用新文件**覆盖**旧解压目录（或删除旧目录后解压新版）
4. 双击新 exe 启动；顶部状态栏显示新版本号

> 覆盖前建议把旧目录的 `version.json` 复制一份备查。

## AI 侧边栏配置

右下角 AI 助手浮标：
- **知识检索**（无需配置）：联网查询芯科官方文档库；离线时提示"知识源不可达（需联网）"
- **对话分析**（需 API key）：点 AI 面板设置，填入 Anthropic / OpenAI / DeepSeek key，
  仅保存在本地 `%APPDATA%\zigbee-analyzer\ai_config.json`，不上传

## 常见问题

- **杀毒软件误报**：PyInstaller 打包的程序偶被误报（无数字签名），
  属正常现象；请添加信任/排除项后使用。
- **中文路径**：工具放在中文目录/桌面可正常运行（已在中文路径实测）；
  个别系统区域设置下如遇异常，建议改英文路径。
- **端口占用**：工具默认自动选择空闲端口，一般无需配置。
- **大包导入慢**：几十 MB 以上大包首次解析需数分钟，属正常；
  支持大包时间窗拆分导入（>30MB 自动提示）。
- **重启后端**：页面顶栏 ⟳ 按钮可重启后端（导入卡死时使用）。

## 版本

当前版本号见解压目录 `version.json`（打包时自动生成，含日期与 git 标识）。
