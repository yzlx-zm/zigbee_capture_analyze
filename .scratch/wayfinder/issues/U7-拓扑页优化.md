# U7-拓扑页优化 — UI 工程

**Assignee:** Claude (2026-08-04) — 认领 (本次范围: 形状分类 + 死控件 + 时间控制升级 + 时间过滤性能)

**What to build:** 拓扑页 (741 行) 其余优化: 渲染性能/路径高亮/过滤交互 (时间控制见 ticket 02)。

**Blocked by:** None

**Status:** ready-for-agent

**Type:** task | **HITL/AFK**

**UI 工程背景**: 见 docs/ui_overview.md (UI 工程 wayfinder 拆分地图)

## Resolution (2026-08-04)

**范围** (用户确认): 设备类型形状分类 + 死控件修复 + 时间控制升级 + 时间过滤性能。交互细节 (tooltip 跟随/工具栏分组) 未选, 留后续。

**产出** (全部在 frontend/js/topo.js + frontend/css/app.css):
1. **形状分类**: 协调器=大六边形 (60px 琥珀, border 3) / 路由器=菱形 (32px 蓝) / 终端=圆 (22px 绿) / 未知=三角 (22px 灰, 新增 node.unknown 样式)
2. **死控件修复**:
   - `#taddr` 输入地址 + Enter → 图上定位 (zoom 1.5× + 路径高亮); `#tgo` 有地址时优先定位 (顺带修复 nodes 页跳转定位从未生效的隐藏 bug)
   - `#tshow-all` → 静默 (孤立 degree=0 非路径) 节点显示/隐藏切换, 按钮 .on 态, 数据刷新/布局切换后状态保持
3. **时间控制升级**: timebar 加 ⏯ 播放按钮 (600ms/步, 步进=窗口 1/4, 到尾自动停) + 时间刻度条 (抓包起止时间 + 当前窗口位置高亮块)
4. **时间过滤性能**: renderGraph 实例复用 — cy.json({elements}) 替换元素, 不再 destroy+重建; 事件只绑定一次 (tooltip/nbt 闭包改模块级 topoNbt); zoom/pan 自然保留

**验证**:
- node --check ✅; headless 无 console 错误 ✅; 新控件渲染 ✅
- 截图 `.scratch/verification/u7-topo/u7_topo.png`: 四色齐全 (琥珀协调器/蓝路由/绿终端/灰未知), 80531 彩色像素
- 交互 (播放/定位/静默切换) 依赖浏览器操作, 待用户实测

**体验优化 (2026-08-05, grilling 确认 5 项, 提交 34a9525)**:
- A1 三层空态引导 (无数据/被过滤/无路由事件, 带操作按钮)
- C1 折叠图例 (📖 按钮 + 浮层: 节点形状 4 类/链路 3 层/状态 2 类)
- C2 邻居边可视化: Link Status 邻居表 → 物理层边 (two-way 点线/one-way 虚线箭头, MCP 语义验证), 与 traffic 边去重, 密度保护 >8 邻居
- C3 邻居聚类排布: off-path 节点按连通分量分组
- D1 拖动松手刷新 + 预览遮罩 (time-mask)
- 后端: events 邻居表按时间窗口重算 (素材验证 7/6/5 设备联动)
- ⚠️ 后端改动需重启后端进程生效

**事故与修复 (2026-08-04, 用户实测发现)**:
- **症状**: 时间线切回拓扑页, 图消失
- **根因**: U7 实例复用重构引入 — 路由切换清空 #mc 后, cy 实例仍绑定已移除的旧容器; 切回时 renderGraph 走复用分支, 新数据渲染进脱离文档的旧容器 → 新页面图空白
- **修复** (提交 29ac16b): reg('topo') 页面重建时销毁旧 cy + 停播放/防抖定时器; playTO/silentHidden 提升模块级跨页面保留
- **验证**: 模拟 进入→切走→切回 (pcap 素材), 切回后 cy-graph 子元素 4 / canvas 3 (修复前 2/0); 用户实测确认 ✅
