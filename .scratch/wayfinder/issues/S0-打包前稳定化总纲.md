# S0 — 打包前界面稳定化总纲 (grilling 对齐 + 自审修正)

**What to build:** 打包分发前逐个界面 code-review + bug 排查, P0/P1 清零, 保证工具稳定。
**Blocked by:** None | **Status:** 总纲 (S1-S7 各界面工单按此执行)

**来源**: 2026-08-26 总控窗口 grilling 对齐 + 自审 6 项修正 (用户确认)。

## 方案 (用户确认)

| 维度 | 结论 |
|------|------|
| 范围 | 每界面 + 连带后端 API/解析器连审 (前后端边界问题定位根因) |
| 顺序 | 使用流程序: S1 导入 → S2 诊断 → S3 拓扑 → S4 报文 → S5 节点 → S6 AI 侧边栏 → S7 顶栏/全局 (S7 最后: app.js/index.html 被所有页面动) |
| 方式 | /code-review skill (规范轴+需求轴) + CDP 运行时实测 + 边界输入 (空数据/大包/未解密/多PAN) |
| 验收 | P0 崩溃/白屏/功能错 (必修) / P1 功能缺陷 (必修) / P2 体验 (记录汇总不阻塞) — P0/P1 清零 + **用户抽验** 即通过 |
| 组织 | 每界面一工单 (审查+修复+复验闭环); 前端文件独立可并行 2-3; **后端共享文件按端点归属, 冲突提前声明** |
| 产出 | 问题清单 (分级) + 修复记录 + 复验证据 (CDP 截图), 写进工单 Resolution |

## 自审修正 (2026-08-26, 6 项)

1. **后端共享文件冲突** → 修复按端点归属, 共享文件改动在工单记录函数/端点范围
2. **需求基线** → 每工单指明: 对应 ticket Resolution + 用户验收记录 + 反馈修正
3. **历史遗留对照** → 审查清单必须对照各 ticket "遗留/已知边界" (bug 候选)
4. **测试素材集** → 每工单指定 (见下)
5. **全局工单时序** → S7 最后 (版本号/导航反复改)
6. **用户抽验** → 每界面关闭前用户快速实测关键路径

## 素材集 (各界面实测用)

| 界面 | 素材 |
|------|------|
| 导入页 | 问题整合五-重点 两大包 (85/76MB) + 1-标准入网抓包-2 (小包) + 群控压测包 (多PAN) |
| 诊断页 | 中继入网抓包(1).cubx + 群控压测包 + 健康素材 |
| 拓扑页 | 中继入网抓包(1) + 群控 (多PAN/16锁) + 卷帘素材 |
| 报文页 | 中继入网抓包(1) (含密文/Leave/路径) |
| 节点页 | dimmer 训练素材 (厂商型号/控制簇) + 群控 (多设备) |
| AI 侧边栏 | 中继入网抓包(1) (范围解析实测) |
| 全局 | 以上轮换 |

## 历史遗留对照清单 (bug 候选, 审查时逐条验证)

- U13: link-history 60s 分段阈值为经验值; A657 判 unknown (素材无证据)
- U14: sleeping 状态未素材实证; offline 角标用粗红边框近似
- U15: 涂鸦 0x0B 下发真实帧待素材; DP 语义表未实证; 0x42 字符串解析已实证
- U16: 上行路径=下行反转 (对称假设, RR 证据不足); pcap 路径 MAC Ack 未实测
- U17: 真流式 LLM 待 key 实测; 范围解析准确度待实测
- 通用: test_p1_contract check 3/4/5 基线失败 (5 项, 非新引入但打包前应评估是否修)

## 工单列表

| 工单 | 界面 | 状态 |
|------|------|------|
| S1 | 导入页 | 首个执行 |
| S2 | 诊断页 | 待 |
| S3 | 拓扑页 | 待 |
| S4 | 报文页 | 待 |
| S5 | 节点页 | 待 |
| S6 | AI 侧边栏 | 待 |
| S7 | 顶栏/全局 | 最后 |
